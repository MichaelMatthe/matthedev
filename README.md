Start Server:

```
cd matthedev_server
npm install
npm start
```

(Server is running on: localhost:7000 see matthedev_server/bin/www)

Start Websocket for SimplexSketcher:

```
cd matthedev_ws
npm install
node index.js
```
